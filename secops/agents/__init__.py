"""Agent runtime contracts for Vantix.

The concrete agent implementations still live in the existing services. This
package defines the stable objects they should exchange so orchestration,
policy, replay, and reporting can reason about actions consistently.
"""

