# Local Topology Example

Copy this structure into `memory/local/MEM.local.md` and replace placeholders. Do not commit the real file.

- API host: `<LOCAL_API_HOST>`
- Optional shared root: `<SHARED_ROOT>`
- Cracking node: `<CRACK_NODE_USER>@<CRACK_NODE_HOST>` using `~/.ssh/<LAB_KEY>`
